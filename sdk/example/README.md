# SDK Example

Contains runnable sample usage of the SDK API.

## File

- `server_example.js`  
  Demonstrates creating a `BraziliaSDK` instance, subscribing to events, and starting server runtime.

## Usage intent

Use this as a quick onboarding reference when embedding the SDK into another Node.js backend.
