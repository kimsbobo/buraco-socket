/**
 * Game Room Status Constants
 */

const GameRoomStatus = {
  WAITING: 'waiting',
  IN_PROGRESS: 'inProgress',
  FINISHED: 'finished',
  ABANDONED: 'abandoned',
};

Object.freeze(GameRoomStatus);

module.exports = GameRoomStatus;
